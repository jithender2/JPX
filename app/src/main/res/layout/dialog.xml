<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
     >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp" >

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="16dp"
            android:text="Multiple Payload Configuration"
            android:textSize="18sp"
            android:textStyle="bold" />

        <!-- Container for dynamically added payload groups -->

        <LinearLayout
            android:id="@+id/container_payload_groups"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical" />

        <!-- Add new payload group button -->

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_add_payload_group"
            android:layout_width="wrap_content"
            android:layout_height="30dp"
            android:layout_margin="4dp"
            android:background="@drawable/rounded_corner"
            android:padding="0dp"
			android:paddingBottom="0dp"
            android:paddingLeft="4dp"
            android:paddingRight="4dp"
            android:paddingTop="0dp"
            android:text="Add Another Payload"
            android:textColor="#000000"
            app:backgroundTint="@null" />

        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_marginBottom="16dp"
            android:background="#DDDDDD" />

        <!-- Save all button -->

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_save_all"
            android:layout_width="wrap_content"
            android:layout_height="30dp"
            android:layout_gravity="center"
            android:background="@drawable/rounded_corner"
            android:paddingBottom="0dp"
            android:paddingLeft="4dp"
            android:paddingRight="4dp"
            android:paddingTop="0dp"
            android:text="Save All"
            android:textColor="#000000"
            app:backgroundTint="@null" />
    </LinearLayout>

</ScrollView>